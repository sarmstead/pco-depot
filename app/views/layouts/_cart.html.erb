<% if cart and not cart.line_items.empty? %>
    <div id="cart" class="basis-4xl border-gray-200 border-1 rounded max-h-fit">
        <%= render cart %>
    </div>
<% else %>
    <div id="cart"></div>
<% end %>